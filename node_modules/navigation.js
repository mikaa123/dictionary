function navigateTo( pageRef ) {
	$('.page').hide();
	$(pageRef).show();
}

module.exports.init = function init( page ) {
	navigateTo(page);

	$('#add-set').on('click', function () {
		navigateTo('.add-set');
	});

	return {
		navigateTo: navigateTo
	};
};

